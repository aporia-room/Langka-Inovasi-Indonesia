*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --kuning: #F5C500;
      --kuning-muda: #FFF3B0;
      --hitam: #111111;
      --hitam-lunak: #222222;
      --abu: #555555;
      --abu-muda: #F7F7F7;
      --putih: #FFFFFF;
      --border: #E5E5E5;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'Plus Jakarta Sans', sans-serif;
      background: var(--putih);
      color: var(--hitam);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }

    /* ── NAVBAR ── */
    .navbar {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      background: var(--hitam);
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 48px;
      height: 64px;
      border-bottom: 3px solid var(--kuning);
    }
    .navbar-logo {
      font-family: 'Instrument Serif', serif;
      font-size: 22px;
      color: var(--putih);
      cursor: pointer;
      letter-spacing: 0.01em;
    }
    .navbar-logo span { color: var(--kuning); }
    .navbar-links { display: flex; gap: 32px; align-items: center; }
    .navbar-links a {
      color: #CCCCCC;
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
      letter-spacing: 0.03em;
      transition: color 0.2s;
      cursor: pointer;
    }
    .navbar-links a:hover, .navbar-links a.active { color: var(--kuning); }
    .navbar-cta {
      background: var(--kuning);
      color: var(--hitam);
      border: none;
      padding: 8px 20px;
      font-size: 13px;
      font-weight: 700;
      cursor: pointer;
      letter-spacing: 0.04em;
      transition: opacity 0.2s;
    }
    .navbar-cta:hover { opacity: 0.85; }

    /* ── MOBILE NAV ── */
    .hamburger {
      display: none;
      flex-direction: column; gap: 5px; cursor: pointer; background: none; border: none; padding: 4px;
    }
    .hamburger span { display: block; width: 24px; height: 2px; background: var(--putih); transition: 0.3s; }
    .mobile-menu {
      display: none; position: fixed; top: 64px; left: 0; right: 0;
      background: var(--hitam); padding: 24px 32px; z-index: 99;
      border-bottom: 3px solid var(--kuning);
    }
    .mobile-menu.open { display: flex; flex-direction: column; gap: 20px; }
    .mobile-menu a {
      color: #CCCCCC; text-decoration: none; font-size: 15px; font-weight: 500; cursor: pointer;
    }
    .mobile-menu a.active, .mobile-menu a:hover { color: var(--kuning); }

    /* ── PAGE WRAPPER ── */
    .page { padding-top: 64px; min-height: 100vh; }

    /* ── SECTION UTILS ── */
    .section { padding: 80px 48px; max-width: 1100px; margin: 0 auto; }
    .section-sm { padding: 60px 48px; max-width: 1100px; margin: 0 auto; }
    .label {
      display: inline-block;
      font-size: 11px; font-weight: 700; letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--hitam);
      background: var(--kuning);
      padding: 4px 10px;
      margin-bottom: 16px;
    }
    .label-outline {
      display: inline-block;
      font-size: 11px; font-weight: 700; letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--kuning);
      border: 1.5px solid var(--kuning);
      padding: 4px 10px;
      margin-bottom: 16px;
    }
    h1.display {
      font-family: 'Instrument Serif', serif;
      font-size: clamp(40px, 6vw, 72px);
      line-height: 1.08;
      letter-spacing: -0.02em;
      color: var(--hitam);
    }
    h2.section-title {
      font-family: 'Instrument Serif', serif;
      font-size: clamp(28px, 4vw, 44px);
      line-height: 1.15;
      letter-spacing: -0.01em;
      color: var(--hitam);
      margin-bottom: 16px;
    }
    h3.card-title {
      font-size: 17px; font-weight: 700; color: var(--hitam); margin-bottom: 8px;
    }
    p.lead {
      font-size: 18px; color: var(--abu); line-height: 1.7; max-width: 600px;
    }
    p.body-text { font-size: 15px; color: var(--abu); line-height: 1.75; }
    .divider { border: none; border-top: 1px solid var(--border); margin: 0; }
    .btn-primary {
      display: inline-block;
      background: var(--kuning);
      color: var(--hitam);
      font-size: 14px; font-weight: 700;
      padding: 14px 28px;
      border: none; cursor: pointer;
      letter-spacing: 0.04em;
      text-decoration: none;
      transition: opacity 0.2s;
    }
    .btn-primary:hover { opacity: 0.85; }
    .btn-outline {
      display: inline-block;
      background: transparent;
      color: var(--hitam);
      font-size: 14px; font-weight: 700;
      padding: 13px 28px;
      border: 2px solid var(--hitam); cursor: pointer;
      letter-spacing: 0.04em;
      text-decoration: none;
      transition: background 0.2s, color 0.2s;
    }
    .btn-outline:hover { background: var(--hitam); color: var(--putih); }

    /* ── HOME ── */
    .hero {
      background: var(--hitam);
      padding: 100px 48px 80px;
      position: relative;
      overflow: hidden;
    }
    .hero::before {
      content: 'LII';
      position: absolute; right: -20px; top: 50%; transform: translateY(-50%);
      font-family: 'Instrument Serif', serif;
      font-size: clamp(160px, 25vw, 300px);
      color: rgba(245,197,0,0.07);
      line-height: 1;
      pointer-events: none;
      user-select: none;
    }
    .hero-inner { max-width: 1100px; margin: 0 auto; position: relative; }
    .hero h1.display { color: var(--putih); margin-bottom: 24px; }
    .hero h1.display em { color: var(--kuning); font-style: italic; }
    .hero .lead { color: #AAAAAA; margin-bottom: 40px; }
    .hero-btns { display: flex; gap: 16px; flex-wrap: wrap; }
    .hero-stats {
      display: grid; grid-template-columns: repeat(3, 1fr);
      gap: 0;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin-top: 72px;
    }
    .stat-item {
      padding: 32px 0;
      border-right: 1px solid rgba(255,255,255,0.1);
    }
    .stat-item:last-child { border-right: none; }
    .stat-num {
      font-family: 'Instrument Serif', serif;
      font-size: 42px; color: var(--kuning);
      line-height: 1;
      margin-bottom: 6px;
    }
    .stat-label { font-size: 13px; color: #888888; letter-spacing: 0.04em; }

    .home-focus { background: var(--abu-muda); }
    .focus-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--border); }
    .focus-card {
      background: var(--putih);
      padding: 36px 32px;
      transition: background 0.2s;
    }
    .focus-card:hover { background: var(--kuning-muda); }
    .focus-num {
      font-family: 'Instrument Serif', serif;
      font-size: 48px; color: var(--kuning);
      line-height: 1; margin-bottom: 16px;
      opacity: 0.6;
    }

    .home-latest { padding: 80px 48px; max-width: 1100px; margin: 0 auto; }
    .latest-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 32px; margin-top: 40px; }
    .latest-card {
      border: 1px solid var(--border);
      padding: 28px;
      transition: border-color 0.2s, transform 0.2s;
      cursor: pointer;
    }
    .latest-card:hover { border-color: var(--kuning); transform: translateY(-3px); }
    .latest-tag {
      font-size: 11px; font-weight: 700; letter-spacing: 0.1em;
      text-transform: uppercase; color: var(--kuning); margin-bottom: 10px;
    }
    .latest-date { font-size: 12px; color: var(--abu); margin-top: 12px; }

    /* ── TENTANG ── */
    .tentang-hero {
      background: var(--hitam);
      padding: 80px 48px;
    }
    .tentang-hero-inner { max-width: 1100px; margin: 0 auto; }
    .tentang-hero h1.display { color: var(--putih); }
    .tentang-hero h1.display em { color: var(--kuning); font-style: italic; }
    .tentang-hero .lead { color: #AAAAAA; margin-top: 20px; }

    .visi-misi-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2px; background: var(--hitam); margin: 0 0 2px 0; }
    .vm-card { background: var(--putih); padding: 48px 40px; }
    .vm-card.kuning-bg { background: var(--kuning); }
    .vm-card.kuning-bg h2.section-title { color: var(--hitam); }
    .vm-card.kuning-bg p { color: var(--hitam-lunak); }

    .tim-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 24px; margin-top: 40px; }
    .tim-card { text-align: center; }
    .tim-avatar {
      width: 80px; height: 80px;
      background: var(--kuning);
      border-radius: 50%;
      margin: 0 auto 16px;
      display: flex; align-items: center; justify-content: center;
      font-family: 'Instrument Serif', serif;
      font-size: 28px; color: var(--hitam);
      font-weight: 700;
    }
    .tim-name { font-size: 15px; font-weight: 700; color: var(--hitam); margin-bottom: 4px; }
    .tim-role { font-size: 12px; color: var(--abu); letter-spacing: 0.04em; text-transform: uppercase; }

    .nilai-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; margin-top: 40px; }
    .nilai-card {
      border-left: 3px solid var(--kuning);
      padding: 24px 24px 24px 28px;
      background: var(--abu-muda);
    }

    /* ── PROGRAM ── */
    .program-hero {
      background: var(--kuning);
      padding: 80px 48px;
    }
    .program-hero-inner { max-width: 1100px; margin: 0 auto; }
    .program-hero h1.display { color: var(--hitam); }
    .program-hero .lead { color: var(--hitam-lunak); margin-top: 20px; }

    .program-list { margin-top: 60px; }
    .program-item {
      display: grid; grid-template-columns: 80px 1fr;
      gap: 32px; align-items: start;
      padding: 40px 0;
      border-bottom: 1px solid var(--border);
    }
    .program-item:first-child { border-top: 1px solid var(--border); }
    .program-num {
      font-family: 'Instrument Serif', serif;
      font-size: 40px; color: var(--kuning);
      line-height: 1; padding-top: 4px;
    }
    .program-tags { display: flex; gap: 8px; flex-wrap: wrap; margin-top: 12px; }
    .tag {
      font-size: 11px; font-weight: 600; letter-spacing: 0.08em; text-transform: uppercase;
      padding: 3px 10px;
      border: 1.5px solid var(--border);
      color: var(--abu);
    }

    .riset-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px; margin-top: 40px; }
    .riset-card {
      padding: 28px;
      background: var(--abu-muda);
      border-top: 3px solid var(--kuning);
    }
    .riset-status {
      display: inline-block;
      font-size: 11px; font-weight: 700; letter-spacing: 0.08em;
      text-transform: uppercase;
      padding: 3px 10px;
      background: var(--hitam);
      color: var(--kuning);
      margin-bottom: 12px;
    }
    .riset-status.selesai { background: var(--kuning); color: var(--hitam); }

    /* ── PUBLIKASI / BLOG ── */
    .blog-hero {
      background: var(--hitam);
      padding: 80px 48px;
    }
    .blog-hero-inner { max-width: 1100px; margin: 0 auto; }
    .blog-hero h1.display { color: var(--putih); }
    .blog-hero h1.display em { color: var(--kuning); font-style: italic; }
    .blog-hero .lead { color: #AAAAAA; margin-top: 20px; }

    .blog-filter { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 40px; }
    .filter-btn {
      font-size: 12px; font-weight: 600; letter-spacing: 0.06em; text-transform: uppercase;
      padding: 8px 18px; background: none; border: 1.5px solid var(--border);
      cursor: pointer; transition: all 0.2s; color: var(--abu);
    }
    .filter-btn.active, .filter-btn:hover {
      background: var(--hitam); color: var(--kuning); border-color: var(--hitam);
    }

    .blog-featured {
      display: grid; grid-template-columns: 1fr 1fr;
      gap: 0; background: var(--hitam); margin-bottom: 40px;
    }
    .blog-featured-img {
      background: var(--kuning);
      min-height: 280px;
      display: flex; align-items: center; justify-content: center;
    }
    .blog-featured-img span {
      font-family: 'Instrument Serif', serif;
      font-size: 80px; color: rgba(17,17,17,0.15);
    }
    .blog-featured-content { padding: 40px; }
    .blog-featured-content .label-outline { margin-bottom: 16px; }
    .blog-featured-content h3 {
      font-family: 'Instrument Serif', serif;
      font-size: 26px; color: var(--putih); line-height: 1.3; margin-bottom: 12px;
    }
    .blog-featured-content p { color: #999999; font-size: 14px; line-height: 1.7; }
    .blog-featured-content .meta { font-size: 12px; color: #666666; margin-top: 20px; }

    .blog-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; }
    .blog-card {
      border: 1px solid var(--border);
      cursor: pointer;
      transition: border-color 0.2s, transform 0.2s;
    }
    .blog-card:hover { border-color: var(--kuning); transform: translateY(-3px); }
    .blog-card-img {
      height: 160px; background: var(--abu-muda);
      display: flex; align-items: center; justify-content: center;
      border-bottom: 1px solid var(--border);
      font-family: 'Instrument Serif', serif;
      font-size: 48px; color: var(--kuning); opacity: 0.4;
    }
    .blog-card-body { padding: 20px; }
    .blog-cat {
      font-size: 10px; font-weight: 700; letter-spacing: 0.1em;
      text-transform: uppercase; color: var(--kuning); margin-bottom: 8px;
    }
    .blog-card-title { font-size: 15px; font-weight: 700; color: var(--hitam); line-height: 1.4; margin-bottom: 8px; }
    .blog-card-desc { font-size: 13px; color: var(--abu); line-height: 1.6; }
    .blog-card-meta { font-size: 11px; color: #BBBBBB; margin-top: 12px; }

    /* ── KONTAK ── */
    .kontak-hero {
      background: var(--kuning);
      padding: 80px 48px;
    }
    .kontak-hero-inner { max-width: 1100px; margin: 0 auto; }
    .kontak-hero h1.display { color: var(--hitam); }
    .kontak-hero .lead { color: var(--hitam-lunak); margin-top: 20px; }

    .kontak-grid { display: grid; grid-template-columns: 1fr 1.4fr; gap: 64px; align-items: start; }
    .kontak-info h3 { font-size: 18px; font-weight: 700; margin-bottom: 24px; }
    .kontak-item { display: flex; gap: 16px; align-items: flex-start; margin-bottom: 28px; }
    .kontak-icon {
      width: 40px; height: 40px; background: var(--kuning);
      display: flex; align-items: center; justify-content: center;
      font-size: 18px; flex-shrink: 0;
    }
    .kontak-item-label { font-size: 11px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: var(--abu); margin-bottom: 4px; }
    .kontak-item-val { font-size: 14px; color: var(--hitam); font-weight: 500; }

    .kontak-form h3 { font-size: 18px; font-weight: 700; margin-bottom: 24px; }
    .form-group { margin-bottom: 20px; }
    .form-group label { display: block; font-size: 12px; font-weight: 700; letter-spacing: 0.06em; text-transform: uppercase; color: var(--hitam); margin-bottom: 8px; }
    .form-group input, .form-group textarea, .form-group select {
      width: 100%; padding: 12px 16px;
      border: 1.5px solid var(--border);
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-size: 14px; color: var(--hitam);
      background: var(--putih);
      outline: none;
      transition: border-color 0.2s;
      appearance: none;
    }
    .form-group input:focus, .form-group textarea:focus, .form-group select:focus { border-color: var(--kuning); }
    .form-group textarea { resize: vertical; min-height: 120px; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }

    .form-submit {
      width: 100%; padding: 16px;
      background: var(--hitam); color: var(--kuning);
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-size: 14px; font-weight: 700; letter-spacing: 0.06em;
      text-transform: uppercase;
      border: none; cursor: pointer;
      transition: background 0.2s;
    }
    .form-submit:hover { background: #333333; }

    .sosmed-grid { display: flex; gap: 12px; margin-top: 32px; }
    .sosmed-btn {
      width: 44px; height: 44px;
      border: 1.5px solid var(--border);
      display: flex; align-items: center; justify-content: center;
      font-size: 18px; cursor: pointer;
      transition: border-color 0.2s, background 0.2s;
      text-decoration: none; color: var(--hitam);
    }
    .sosmed-btn:hover { border-color: var(--kuning); background: var(--kuning); }

    /* ── FOOTER ── */
    .footer { background: var(--hitam); padding: 60px 48px 32px; }
    .footer-inner { max-width: 1100px; margin: 0 auto; }
    .footer-top { display: grid; grid-template-columns: 1.5fr 1fr 1fr; gap: 48px; margin-bottom: 48px; }
    .footer-brand { }
    .footer-logo {
      font-family: 'Instrument Serif', serif;
      font-size: 22px; color: var(--putih); margin-bottom: 12px;
    }
    .footer-logo span { color: var(--kuning); }
    .footer-desc { font-size: 13px; color: #666666; line-height: 1.7; max-width: 280px; }
    .footer-col h4 { font-size: 11px; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--kuning); margin-bottom: 16px; }
    .footer-col a { display: block; font-size: 13px; color: #888888; text-decoration: none; margin-bottom: 10px; cursor: pointer; transition: color 0.2s; }
    .footer-col a:hover { color: var(--putih); }
    .footer-bottom { border-top: 1px solid #222222; padding-top: 24px; display: flex; justify-content: space-between; align-items: center; }
    .footer-copy { font-size: 12px; color: #444444; }
    .footer-kuning { color: var(--kuning); }

    .toast {
      position: fixed; bottom: 32px; right: 32px; z-index: 999;
      background: var(--hitam); color: var(--kuning);
      padding: 16px 24px;
      font-size: 14px; font-weight: 600;
      border-left: 4px solid var(--kuning);
      animation: slideIn 0.3s ease;
    }
    @keyframes slideIn { from { transform: translateX(120%); opacity: 0; } to { transform: translateX(0); opacity: 1; } }

    /* ── RESPONSIVE ── */
    @media (max-width: 900px) {
      .navbar { padding: 0 24px; }
      .navbar-links { display: none; }
      .hamburger { display: flex; }
      .section, .section-sm, .hero, .tentang-hero, .program-hero, .blog-hero, .kontak-hero, .footer { padding-left: 24px; padding-right: 24px; }
      .hero-stats { grid-template-columns: 1fr; }
      .stat-item { border-right: none; border-bottom: 1px solid rgba(255,255,255,0.1); padding: 24px 0; }
      .visi-misi-grid { grid-template-columns: 1fr; }
      .tim-grid { grid-template-columns: repeat(2, 1fr); }
      .nilai-grid { grid-template-columns: 1fr; }
      .focus-grid { grid-template-columns: 1fr; }
      .riset-grid { grid-template-columns: 1fr; }
      .latest-grid { grid-template-columns: 1fr; }
      .blog-featured { grid-template-columns: 1fr; }
      .blog-featured-img { min-height: 160px; }
      .blog-grid { grid-template-columns: 1fr; }
      .kontak-grid { grid-template-columns: 1fr; gap: 40px; }
      .form-row { grid-template-columns: 1fr; }
      .footer-top { grid-template-columns: 1fr; gap: 32px; }
      .footer-bottom { flex-direction: column; gap: 8px; text-align: center; }
      .program-item { grid-template-columns: 1fr; gap: 8px; }
    }
/* ── NEXT.JS LINK RESET ── */
a.latest-card, a.btn-primary, a.btn-outline, a.navbar-cta, a.navbar-logo {
  text-decoration: none;
  color: inherit;
}
a.btn-primary { color: var(--hitam); }
a.btn-outline { color: var(--hitam); }
a.navbar-cta { color: var(--hitam); }
a.navbar-logo { color: var(--putih); }
